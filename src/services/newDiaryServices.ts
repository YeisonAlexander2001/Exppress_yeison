import diaryData from './diaries.json'
import { DiaryEntry } from './types'

const ID = diaryData as DiaryEntry[]

const nuevoid = ID[0]

const nuevoDiario = ['nuevoid', 'WEATHER', 'COMMENT', 'VISIBILITY']
